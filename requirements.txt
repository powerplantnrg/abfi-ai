# ABFI Intelligence Suite - Requirements for Vercel deployment
# For local development with full ML: pip install -r requirements-full.txt

# Core API
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# HTTP Client
httpx>=0.26.0

# Database - Turso (LibSQL) for serverless production
libsql-experimental>=0.0.47

# Utilities
python-dotenv>=1.0.0
python-multipart>=0.0.6
